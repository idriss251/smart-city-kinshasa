import {useForm} from 'react-hook-form'; import {api} from '../../services/api'; export default function Login(){const {register,handleSubmit}=useForm(); const onSubmit=async(d:any)=>{const r=await api.post('/api/auth/login',d); localStorage.setItem('token',r.data.token); location.href='/dashboard'}; return <form className='panel max-w-sm mx-auto mt-10 space-y-3' onSubmit={handleSubmit(onSubmit)}><h1 className='text-xl font-bold'>Connexion</h1><input className='input' placeholder='username' {...register('username')}/><input className='input' type='password' placeholder='password' {...register('password')}/><button className='btn'>Entrer</button></form>}
